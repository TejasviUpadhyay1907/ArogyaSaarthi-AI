PORT=4000
AI_ENGINE_URL=http://localhost:8000
DATABASE_FILE=./db/arogya.db
NODE_ENV=development

# Firebase Admin SDK — one of the following is required:
# Option A: paste the full service account JSON as a single-line string
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Option B: path to the service account JSON file
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Firebase project ID (required if using Application Default Credentials)
FIREBASE_PROJECT_ID=your-firebase-project-id
